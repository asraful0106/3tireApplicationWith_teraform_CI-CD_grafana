# Copy to .env and fill in values — never commit .env to git

# PostgreSQL EC2 private IP (output from terraform apply)
DB_HOST=10.0.20.x
DB_PORT=5432
DB_USER=appuser
DB_PASSWORD=your_strong_password
DB_NAME=appdb

APP_PORT=3000
NODE_ENV=development
